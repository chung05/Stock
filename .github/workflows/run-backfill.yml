name: 🚀 手動執行 MACD 歷史補件

on:
  workflow_dispatch: # 這個設定代表允許我們在網頁上手動點擊按鈕執行

jobs:
  run-backfill:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 取得最新程式碼
        uses: actions/checkout@v4.2.2 # 💡 修正原本的 v3 舊核心

      - name: ⚙️ 設定 Node.js 環境
        uses: actions/setup-node@v4
        with:
          node-version: '20' # 💡 從舊版 18 強制升級到與你日常環境一致的 20

      - name: 📦 安裝必要套件
        run: npm install @supabase/supabase-js dotenv

      - name: ▶️ 執行 MACD 補件腳本
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node backfill-macd.js
